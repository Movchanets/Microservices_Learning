name: CI

on:
  push:
    branches:
      - "**"
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: "10.0.x"

      - name: Restore dependencies
        run: dotnet restore Marketplace.slnx

      - name: Build
        run: dotnet build Marketplace.slnx --no-restore -c Release

  changed-tests:
    runs-on: ubuntu-latest
    needs: build
    if: >
      (github.event_name == 'push' && github.ref_name != 'main' && github.ref_name != 'master') ||
      (github.event_name == 'pull_request' && github.base_ref != 'main' && github.base_ref != 'master')

    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: "10.0.x"

      - name: Detect affected test areas
        id: filters
        uses: dorny/paths-filter@v3
        with:
          list-files: shell
          filters: |
            buildingblocks_unit:
              - 'src/BuildingBlocks/**'
              - 'tests/UnitTests/BuildingBlocks.Infrastructure.UnitTests/**'
            identity_unit:
              - 'src/Microservices/Identity/**'
              - 'tests/UnitTests/Identity.UnitTests/**'
            identity_integration:
              - 'src/Microservices/Identity/**'
              - 'tests/IntegrationTests/Identity.IntegrationTests/**'
              - 'tests/IntegrationTests/Shared/**'
            api_gateway_tests:
              - 'src/Gateways/ApiGateway/**'
              - 'tests/UnitTests/ApiGateway.UnitTests/**'
              - 'tests/IntegrationTests/ApiGateway.IntegrationTests/**'
            unit_tests:
              - 'tests/UnitTests/**'

            source_changes:
              - 'src/**'

      - name: Run affected tests
        shell: bash
        run: |
          set -euo pipefail

          declare -A projects=()

          add_project() {
            local project="$1"
            if [ -f "$project" ]; then
              projects["$project"]=1
            fi
          }

          if [ "${{ steps.filters.outputs.buildingblocks_unit }}" = "true" ]; then
            add_project "tests/UnitTests/BuildingBlocks.Infrastructure.UnitTests/BuildingBlocks.Infrastructure.UnitTests.csproj"
          fi

          if [ "${{ steps.filters.outputs.identity_unit }}" = "true" ]; then
            add_project "tests/UnitTests/Identity.UnitTests/Identity.UnitTests.csproj"
          fi

          if [ "${{ steps.filters.outputs.unit_tests }}" = "true" ]; then
            while IFS= read -r project; do
              add_project "$project"
            done < <(find tests/UnitTests -type f -name '*.csproj')
          fi

          if [ "${{ steps.filters.outputs.identity_integration }}" = "true" ]; then
            add_project "tests/IntegrationTests/Identity.IntegrationTests/Identity.IntegrationTests.csproj"
          fi

          if [ "${{ steps.filters.outputs.api_gateway_tests }}" = "true" ]; then
            add_project "tests/UnitTests/ApiGateway.UnitTests/ApiGateway.UnitTests.csproj"
            add_project "tests/IntegrationTests/ApiGateway.IntegrationTests/ApiGateway.IntegrationTests.csproj"
          fi

          if [ "${{ steps.filters.outputs.integration_tests }}" = "true" ]; then
            while IFS= read -r project; do
              add_project "$project"
            done < <(find tests/IntegrationTests -type f -name '*.csproj')
          fi

          if [ "${{ steps.filters.outputs.source_changes }}" = "true" ]; then
            while IFS= read -r project; do
              add_project "$project"
            done < <(find tests/IntegrationTests -type f -name '*.csproj')
          fi

          if [ "${#projects[@]}" -eq 0 ]; then
            echo "No affected test projects found."
            exit 0
          fi

          echo "Detected test projects:"
          printf '%s\n' "${!projects[@]}" | sort

          for project in "${!projects[@]}"; do
            if [[ "$project" == tests/IntegrationTests/Shared/* ]]; then
              echo "Skipping shared helper project $project"
              continue
            fi

            echo "Running tests for $project"
            dotnet test "$project" -c Release
          done

  full-tests:
    runs-on: ubuntu-latest
    needs: build
    if: >
      (github.event_name == 'push' && (github.ref_name == 'main' || github.ref_name == 'master')) ||
      (github.event_name == 'pull_request' && (github.base_ref == 'main' || github.base_ref == 'master'))

    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: "10.0.x"

      - name: Run unit tests
        shell: bash
        run: |
          set -euo pipefail
          while IFS= read -r project; do
            echo "Running unit tests for $project"
            dotnet test "$project" -c Release
          done < <(find tests/UnitTests -type f -name '*.csproj' | sort)

      - name: Run integration tests
        shell: bash
        run: |
          set -euo pipefail
          while IFS= read -r project; do
            if [[ "$project" == tests/IntegrationTests/Shared/* ]]; then
              echo "Skipping shared helper project $project"
              continue
            fi

            echo "Running integration tests for $project"
            dotnet test "$project" -c Release
          done < <(find tests/IntegrationTests -type f -name '*.csproj' | sort)

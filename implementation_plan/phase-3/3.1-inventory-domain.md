# 3.1 — Inventory.Domain

> **Ref**: [plans/03-clean-architecture.md](../../plans/03-clean-architecture.md) · [plans/02-domain-decomposition.md](../../plans/02-domain-decomposition.md)

## Goal
Create the Inventory domain layer with `InventoryItem` aggregate and related value objects/events. Pure C# domain logic only.

## Prerequisites
- Sub-plan 3.0 completed

## Steps

### Step 1: Create the project

```powershell
dotnet new classlib --name Inventory.Domain --output src/Microservices/Inventory/Inventory.Domain --framework net10.0
dotnet sln Marketplace.sln add src/Microservices/Inventory/Inventory.Domain/Inventory.Domain.csproj
Remove-Item src/Microservices/Inventory/Inventory.Domain/Class1.cs -ErrorAction SilentlyContinue
dotnet add src/Microservices/Inventory/Inventory.Domain/Inventory.Domain.csproj reference src/BuildingBlocks/SharedContracts/BuildingBlocks.SharedContracts.csproj
```

### Step 2: Create folder structure

```powershell
New-Item -ItemType Directory -Force -Path `
  "src/Microservices/Inventory/Inventory.Domain/Aggregates", `
  "src/Microservices/Inventory/Inventory.Domain/Events", `
  "src/Microservices/Inventory/Inventory.Domain/Exceptions"
```

### Step 3: Create Domain Exceptions

File: `src/Microservices/Inventory/Inventory.Domain/Exceptions/OutOfStockException.cs`
```csharp
namespace Inventory.Domain.Exceptions;

public class OutOfStockException : Exception
{
    public OutOfStockException(string sku, int requested, int available) 
        : base($"Insufficient stock for SKU {sku}. Requested: {requested}, Available: {available}")
    {
    }
}
```

### Step 4: Create Domain Events

File: `src/Microservices/Inventory/Inventory.Domain/Events/StockReservedDomainEvent.cs`
```csharp
using BuildingBlocks.SharedContracts.Abstractions;

namespace Inventory.Domain.Events;

public sealed record StockReservedDomainEvent(
    Guid InventoryItemId, 
    string Sku, 
    int Quantity) : IDomainEvent;
```

File: `src/Microservices/Inventory/Inventory.Domain/Events/StockReleasedDomainEvent.cs`
```csharp
using BuildingBlocks.SharedContracts.Abstractions;

namespace Inventory.Domain.Events;

public sealed record StockReleasedDomainEvent(
    Guid InventoryItemId, 
    string Sku, 
    int Quantity) : IDomainEvent;
```

### Step 5: Create `InventoryItem` aggregate root

File: `src/Microservices/Inventory/Inventory.Domain/Aggregates/InventoryItem.cs`
```csharp
using BuildingBlocks.SharedContracts.Abstractions;
using Inventory.Domain.Events;
using Inventory.Domain.Exceptions;

namespace Inventory.Domain.Aggregates;

public sealed class InventoryItem : AggregateRoot
{
    public string Sku { get; private set; } = string.Empty;
    public int AvailableQuantity { get; private set; }
    public byte[] Version { get; private set; } = []; // For optimistic concurrency

    private InventoryItem() { }

    public static InventoryItem Create(string sku, int initialQuantity)
    {
        ArgumentException.ThrowIfNullOrWhiteSpace(sku);
        if (initialQuantity < 0)
            throw new ArgumentException("Initial quantity cannot be negative", nameof(initialQuantity));

        return new InventoryItem
        {
            Sku = sku.Trim().ToUpperInvariant(),
            AvailableQuantity = initialQuantity
        };
    }

    public void AddStock(int quantity)
    {
        if (quantity <= 0)
            throw new ArgumentException("Quantity to add must be positive", nameof(quantity));

        AvailableQuantity += quantity;
    }

    public void Reserve(int quantity)
    {
        if (quantity <= 0)
            throw new ArgumentException("Quantity to reserve must be positive", nameof(quantity));

        if (AvailableQuantity < quantity)
            throw new OutOfStockException(Sku, quantity, AvailableQuantity);

        AvailableQuantity -= quantity;
        
        AddDomainEvent(new StockReservedDomainEvent(Id, Sku, quantity));
    }

    public void Release(int quantity)
    {
        if (quantity <= 0)
            throw new ArgumentException("Quantity to release must be positive", nameof(quantity));

        AvailableQuantity += quantity;
        
        AddDomainEvent(new StockReleasedDomainEvent(Id, Sku, quantity));
    }
}
```

### Step 6: Create repository interface

File: `src/Microservices/Inventory/Inventory.Domain/Aggregates/IInventoryItemRepository.cs`
```csharp
using BuildingBlocks.SharedContracts.Abstractions;

namespace Inventory.Domain.Aggregates;

public interface IInventoryItemRepository : IRepository<InventoryItem>
{
    Task<InventoryItem?> GetBySkuAsync(string sku, CancellationToken ct = default);
    Task<List<InventoryItem>> GetBySkusAsync(IEnumerable<string> skus, CancellationToken ct = default);
}
```

## Verification
```powershell
dotnet build src/Microservices/Inventory/Inventory.Domain/Inventory.Domain.csproj
```

## Done When
- [ ] `Inventory.Domain` project exists and references only `SharedContracts`
- [ ] `InventoryItem` aggregate supports `AddStock`, `Reserve`, `Release` and optimistic concurrency Version field
- [ ] Domain events and exceptions created
- [ ] `IInventoryItemRepository` interface defined
- [ ] `dotnet build` succeeds